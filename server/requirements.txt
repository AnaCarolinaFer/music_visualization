# Python dependencies
# IMPORTANTE: Use Python 3.11 (venv já configurado com mise)
# Usar versões específicas para compatibilidade com Spleeter
flask==2.0.3
flask-cors
werkzeug==2.0.3
click==7.1.2
spleeter>=2.3

# System dependency (install with apt):
# sudo apt-get install ffmpeg
